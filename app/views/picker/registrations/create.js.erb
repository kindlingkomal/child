<% if resource.errors.any? %>
  $(".devise-signup-form-partner").html("<%= j(render 'picker/registrations/form', resource: resource, resource_name: resource_name) %>");
  $('#signupForm').parsley();
<% else %>
  window.location.reload();
<% end %>
