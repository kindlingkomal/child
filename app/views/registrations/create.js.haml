- if resource.errors.any?
  $("#sign-log .modal-body .devise-logout-form").html("#{escape_javascript(render partial: "registrations/form", locals: {resource: resource, resource_name: resource_name})}");
  $('#signupForm').parsley();
- else
  window.location.reload();
